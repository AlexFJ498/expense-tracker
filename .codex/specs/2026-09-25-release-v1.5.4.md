# v1.5.4 optional import classification release

## Intent
- Goal: publish the completed import fix as signed Expense Tracker v1.5.4.
- User value: imported movements can retain an unspecified necessary classification, including after save and reopen.
- Non-goals: change analytics semantics, alter the updater contract, add platforms, or change signing credentials.

## Slice
- Product behavior: use the existing optional-classification fix and regression tests in the import slice; do not expand it for this release.
- Version metadata: align npm package and lockfile, Cargo package and lockfile, and Tauri config to `1.5.4`.
- Release pipeline: retain the existing secret-free PR checks, signed manual `main` preflight, and tag-triggered publication.
- Updater: publish the existing Windows x64, Linux x64, and macOS arm64 assets and signatures with `latest.json` for `v1.5.4`.

## Context To Read
- Must read: `.codex/specs/2026-05-08-import-data.md`, `.github/workflows/release.yml`, `.codex/checklists/publication-gate.md`, and the five version metadata files.
- Do not read: signing secrets, private attachments, real workbooks, bank exports, or generated build folders.

## Contract
- `package.json`, the package-lock root and package entry, `src-tauri/Cargo.toml`, the expense-tracker entry in `src-tauri/Cargo.lock`, and `src-tauri/tauri.conf.json` declare `1.5.4`.
- The release tag is `v1.5.4` and targets the merge commit verified by the signed manual `main` preflight.
- `latest.json` declares version `1.5.4` and contains complete URLs and signatures for `windows-x86_64`, `linux-x86_64`, and `darwin-aarch64`.
- A PR never receives signing credentials. Publication remains limited to a `v*` tag push after signed preflight succeeds.

## Tests First
- No new behavior is introduced by the version bump. The import change already had observed frontend RED/GREEN and a Rust save/reopen regression test.
- Local checks: release manifest and workflow Node tests, `npm test`, `npm run build`, `cargo test`, `npm run tauri build`, `npm audit`, and the publication/security gate; report any unavailable check honestly.
- CI checks: pass secret-free PR validation, then inspect signed platform builds and manifest validation from manual `main` dispatch before tagging.
- Published artifact check: inspect the GitHub Release assets and parse `latest.json` only after the tag workflow succeeds.

## Acceptance
- The import fix and aligned versions reach `main` through the reviewed PR with required checks passing.
- Signed preflight succeeds at the intended merge commit before `v1.5.4` is pushed.
- The release has supported installers, update bundles, matching signatures, and a valid cross-platform updater manifest.

## Decision Log
- 2026-09-25: Reuse the existing secure release workflow; limit this release slice to the optional import fix and synchronized version metadata.
